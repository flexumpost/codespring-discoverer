

## Ændring: Sorter log i omvendt rækkefølge

**`src/components/MailItemLogSheet.tsx` — linje 81**

Ændr `ascending: true` til `ascending: false` i `.order("created_at", ...)` kaldet, så nyeste handlinger vises øverst.

